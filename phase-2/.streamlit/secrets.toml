# Streamlit Cloud secrets/config for Phase 2
# Copy these values into Streamlit Cloud's Secrets Manager.

GROQ_API_KEY = "your_groq_api_key_here"
API_SECRET_KEY = "your_random_secret_key_here"

# Raw dataset mapping
# Use the raw input folder `Data/` and the generated output folder `data/`.
DATA_RAW_PATH = "Data"
DATA_PROCESSED_PATH = "data/processed"
MODELS_PATH = "models"
RAG_INDEX_PATH = "rag/faiss_index.bin"
RAG_METADATA_PATH = "rag/metadata.json"
RAG_DOCUMENTS_PATH = "rag/documents"
